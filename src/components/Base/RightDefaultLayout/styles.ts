import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => {
  return {
    rightLayout: {
      width: '100%',
      minHeight: '90vh'
      // padding: '20px 20px',
      // background: '#000',
    },
  };
});

export default useStyles;
