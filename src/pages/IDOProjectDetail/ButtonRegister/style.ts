import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme: any) => {
    return {
        root: {
            backgroundColor: "#8203D0",
            borderRadius: "60px",
            fontSize: "14px",
            color: "#ffffff",
            textTransform: 'none',
            padding: '8px 42px',
            fontWeight: 'bold'
        },
    }
})